<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:behaviors="clr-namespace:SendEmail.Validation"
             x:Class="SendEmail.SendEmail"
             Title="Newsletter">
    
    <ContentPage.Content>
         
        
        <Grid Padding="0">
            

            <Image
            Aspect="AspectFill"
            Source="backGround.png"
            RelativeLayout.WidthConstraint= "{ConstraintExpression Type=RelativeToParent, Property=Width}"
            RelativeLayout.HeightConstraint= "{ConstraintExpression Type=RelativeToParent, Property=Height}">
        </Image>


            <StackLayout Padding="20" Spacing="10" Orientation="Vertical">
            <Label Text="Sign Up To Enews" XAlign="Center" Margin="15" HorizontalOptions="Center" FontAttributes="Bold" FontSize="22"/>
            <Label Text="Sign up to our email newsletter today and get all the latest news, updates, information and ways to get involved straight to your inbox!" XAlign="Center" Margin="15" HorizontalOptions="Center" FontSize="14"/>
            <Entry Keyboard="Text" Placeholder="First name" FontSize="15" x:Name="firstName">
            <Entry.Behaviors>
                <behaviors:LetterValidatorBehavior/>
            </Entry.Behaviors>
            </Entry>    
            <Entry Keyboard="Text" Placeholder="Last name" FontSize="15" x:Name="lastName">
            <Entry.Behaviors>
                <behaviors:LetterValidatorBehavior/>
            </Entry.Behaviors>
            </Entry>
            <Entry Keyboard="Telephone" Placeholder="e.g. 0211235678" FontSize="15" x:Name="phoneNumber">
            <Entry.Behaviors>
                <behaviors:NumberValidationBehavior/>
            </Entry.Behaviors>
            </Entry>
            <Entry Keyboard="Email" Placeholder="sample@email.com" FontSize="15" x:Name="EmailEntry">
            <Entry.Behaviors>
                <behaviors:EmailValidatorBehavior/>
            </Entry.Behaviors>
            </Entry>
            
            <Button Text="Subscribe" Clicked="Handle_Clicked" />
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
